// ignore_for_file: unnecessary_string_interpolations, non_constant_identifier_names, unused_local_variable, prefer_typing_uninitialized_variables

import 'package:flutter/material.dart';
import '../db/sql_helper.dart';

class MemberPage extends StatefulWidget {

  String username;
  String password;

  MemberPage({super.key, required this.username, required this.password});

  @override
  State<MemberPage> createState() => _MemberPageState();
}


class _MemberPageState extends State<MemberPage> {

   bool _isLoading = true;


  // This function is used to fetch all data from the database
  @override
  void initState() {
    super.initState();
    _refreshUserLoginmembercard(); // Loading the data when the page starts
  }

  // This function is used to fetch data from the database

  
      // Update an existing password Membercard by kode_member
  Future<void> _updateDataPasswordmembercard(int kode_member) async {
    await SQLHelper.updatePasswordmembercard(
        kode_member,
        password);
        _refreshUserLoginmembercard();
  }


    @override
  Widget build(BuildContext context) {

    if (_isLoading) {
      return const Center(
        child: CircularProgressIndicator(),
      );
    } else {
      return Scaffold(
      backgroundColor: Colors.green,
      appBar: AppBar(
        title: const Text('Member Page'),
        backgroundColor: Colors.green.shade700,
      ),
      // Add your member page content here

      body: ListView(
        children: [
          
          ListTile(
            leading: const Icon(Icons.info_rounded),
            title: const Text("Kode Member"),
            subtitle: Text(kode_member.toString()),
            tileColor: Colors.green.shade300,
          ),
          ListTile(
            leading: const Icon(Icons.person),
            title: const Text("Nama"),
            subtitle: Text(nama.toString()),
            tileColor: Colors.green.shade400,
          ),
          ListTile(
            leading: const Icon(Icons.date_range),
            title: const Text("Tanggal lahir"),
            subtitle: Text(tanggal_lahir.toString()),
            tileColor: Colors.green.shade300,
          ),
          ListTile(
            leading: const Icon(Icons.location_city),
            title: const Text("Alamat"),
            subtitle: Text(alamat.toString()),
            tileColor: Colors.green.shade400,
          ),
          ListTile(
            leading: const Icon(Icons.girl_outlined),
            title: const Text("Jenis Kelamin"),
            subtitle: Text(jenis_kelamin.toString()),
            tileColor: Colors.green.shade300,
          ),
          ListTile(
            leading: const Icon(Icons.key),
            title: const Text("Username"),
            subtitle: Text('${widget.username}'),
            tileColor: Colors.green.shade400,
          ),
          ListTile(
            leading: const Icon(Icons.password),
            title: const Text("Password"),
            subtitle: Text(password.toString()),
            tileColor: Colors.green.shade300,
            trailing: IconButton(onPressed: () {}, icon: const Icon(Icons.edit))
          ),
        ],
      ),
    );
  } 
}
}


